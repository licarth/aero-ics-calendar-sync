{
  "name": "aerogest-calendar",
  "version": "1.0.0",
  "description": "CalDAV server for aerogest flights",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.1.3"
  },
  "functions": {
    "predeploy": "npm --prefix functions/flights run build"
  }
}
